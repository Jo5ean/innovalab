---
import config from "../../config/config.ts";
const { title } = config;
const titleArr = title.split(" | ");
const titleNameArr = titleArr[0].split(" ");
const titleName = titleNameArr[0];
const subTitleName = titleNameArr[1];
---

<div
    class="bg-white w-full flex flex-row items-center justify-start px-12 py-5 sticky top-0 z-50 gap-x-5"
>
    <img
        src="/logos/logo.png"
        alt="logo"
        class="w-auto h-14 lg:h-20 object-contain"
    />
    <h1 class="text-base md:text-xl font-medium tracking-tight">{titleName} <b class="font-extrabold">{subTitleName} &reg;</b></h1>
</div>
