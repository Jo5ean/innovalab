---
import config from "@/config/config";
---

<section
    class="snap-start flex flex-col w-full md:container mx-auto px-10 md:px-24 items-start md:items-stretch justify-center md:min-h-screen mt-8 md:-mt-32 gap-y-10 mb-0"
>
    <h1 class="hidden">{config.title}</h1>
    <h2 class="text-5xl md:text-6xl">
        Creator and agency payments <b class="font-bold italic"
            >made effortless.</b
        >
    </h2>
    <div class="space-y-1 md:space-y-0">
        <p class="leading-8 md:leading-normal text-xl md:text-2xl">
            Influencer Funds helps you send fast, secure, and trackable payments
            to your marketing vendors.
        </p>
        <p
            class="text-xl md:text-2xl flex flex-col md:flex-row gap-y-5 md:gap-y-0 md:gap-x-5 items-start md:items-center justify-start"
        >
            Anytime, anywhere.
            <button
                class="flex flex-row items-center gap-x-5 text-xl justify-between bg-gradient-to-r from-primary to-secondary text-black py-1 px-6 rounded-l-full rounded-br-full border border-black"
            >
                Schedule a demo
                <svg
                    class="w-6 h-6"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                    xmlns="http://www.w3.org/2000/svg"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M5 12h14m-7-7l7 7-7 7"></path>
                </svg>
            </button>
        </p>
    </div>
</section>

<style>
    button {
        box-shadow: 8px 8px 1px #000000;
    }
    @media screen and (max-width: 380px) {
        h2 {
            @apply text-4xl;
        }
    }
</style>
