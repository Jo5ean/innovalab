<div class="slide11-container">
  <div class="background-image"></div>
  <div class="slide11-content">
    <h2 class="slide11-title">Legajo Digital</h2>
    <div class="slide11-columns">
      <div class="slide11-col">
        <div class="slide11-text-container">
          <div class="slide11-text">
            <p>Se solicita cargar la siguiente documentación:</p>
            <ul class="list-disc">
              <li>DNI (de ambos lados)</li>
              <li>Cuil</li>
              <li>Partida de Nacimiento (de ambos lados)</li>
              <li>Título Secundario / Terciario (de ambos lados)</li>
            </ul>
          </div>
          <div class="slide11-img">
            <img src="/images/slides/slide111.jpg" alt="Carga de documentos obligatorios" />
          </div>
        </div>
      </div>
      <div class="slide11-col">
        <div class="slide11-text-container">
          <div class="slide11-text">
            <p>Para una mejor carga de archivos, te sugerimos la aplicación <b>Cam Scanner</b>, que permite digitalizar tu documentación y cargarla en formato PDF.</p><br>
            <p>Puedes descargarlo escaneando el QR:</p>
          </div>
          <div class="slide11-img">
            <img src="/images/slides/slide112.png" alt="QR Cam Scanner" />
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
.slide11-container {
  width: 100vw;
  height: 100vh;
  position: relative;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
}
.background-image {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image: url('/images/fondo.jpg');
  background-size: cover;
  background-position: center;
  z-index: 0;
  filter: brightness(0.7);
}
.slide11-content {
  position: relative;
  z-index: 1;
  width: 92vw;
  max-width: 1100px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: 18px;
}
.slide11-title {
  color: #e13e3e;
  font-size: 2.2rem;
  font-weight: 900;
  margin-bottom: 10px;
  font-family: 'Montserrat', Arial, sans-serif;
  letter-spacing: 0.5px;
}
.slide11-columns {
  display: flex;
  flex-direction: row;
  gap: 32px;
  width: 100%;
  justify-content: space-between;
}
.slide11-col {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
}
.slide11-text-container {
  background: rgba(23, 21, 21, 0.5);
  border-radius: 16px;
  padding: 24px 32px;
  box-shadow: 0 4px 18px rgba(0,0,0,0.10);
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: 18px;
}
.slide11-text {
  color: #fff;
  font-size: 1.08rem;
  font-family: 'Montserrat', Arial, sans-serif;
  font-weight: 400;
  line-height: 1.5;
}
.slide11-text ul {
  margin: 8px 0 0 18px;
  padding: 0;
}
.slide11-text li {
  margin-bottom: 2px;
}
.slide11-img {
  width: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
}
.slide11-img img {
  max-width: 200px;
  width: 100%;
  border-radius: 8px;
  background: #fff;
  box-shadow: 0 2px 8px rgba(0,0,0,0.10);
  padding: 6px;
}
@media (max-width: 900px) {
  .slide11-content {
    width: 99vw;
    padding: 12px 0 0 0;
    gap: 10px;
  }
  .slide11-columns {
    flex-direction: column;
    gap: 18px;
  }
  .slide11-text-container {
    padding: 16px 18px;
  }
  .slide11-img img {
    max-width: 65vw;
  }
}
@media (max-width: 600px) {
  .slide11-content {
    padding: 4px 0 0 0;
    gap: 6px;
  }
  .slide11-title {
    font-size: 1.3rem;
  }
  .slide11-text-container {
    padding: 12px 10px;
  }
  .slide11-img img {
    max-width: 85vw;
    padding: 2px;
  }
  .slide11-text {
    font-size: 0.92rem;
  }
}
</style> 
